export const amuletAlreadyRecievedMessage = async lang => {
	if (lang === 'ru') {
		return `✅Амулет уже получен, проверьте инвентарь`
	} else {
		return `✅Amulet has already been recieved, check your inventory`
	}
}
